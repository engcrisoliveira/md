Para inicializar garanta que o terminal integrado esteja na pasta jupyter, se não estiver execute o comando `cd jupyter`.

Para inicializar a venv execute o seguinte comando `.\venv\Scripts\Activate.ps1`.

PS: as backticks são apenas para delimitar o comando, não copiar elas para o terminal.